export interface ValuedClient{
      date:string,
      name:string,
      domain:string,
      category:string,
      feedback:string
}

export interface ValuedClientProps{
      card: ValuedClient
}