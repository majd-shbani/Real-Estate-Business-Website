export interface TextContainerProps {
    heading: string;
    Paragraph: string;
    fullWidth?: boolean;
    isLast?: boolean;
}

export interface TitleProps {
    title: string
    description: string
}

export interface SerivceCardProps {
    logo: string
    alt: string
    titleService: string
}
